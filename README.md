### To Run:

`npm install`

`npm run db:setup`

`npm start`

### To test:

`curl http://localhost:3301/api/team` via terminal or paste the link in a browser

### Guidelines:

src/index.json has a GET route already defined

Primary task: implement a way to parse and display this information

Secondary task: Develop more capabilities like creating, updating, and deleting records in the database

Bonus task: Tell us about tactics that can be used to help deliver this app at scale
